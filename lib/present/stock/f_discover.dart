import 'package:flutter/material.dart';

class DiscoveryFrag extends StatelessWidget {
  const DiscoveryFrag({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}